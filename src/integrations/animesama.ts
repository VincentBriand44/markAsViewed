import type { IntegrationData } from "../types";

const position = document;

const integration = (): IntegrationData => {
	throw new Error("not implemented");
};

export default { integration, position };
